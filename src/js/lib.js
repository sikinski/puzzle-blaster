// getImageData
/*
const checkNearlyCubes = () => {
          clickedCubes.push(clickedCube)
          if (clickedCubes.length > numNearly) {
            // Check left
            if (leftCube && clickedCube == leftCube && !clickedCubes.includes(leftCube)) {
              clickedCubes.push(leftCube)
              numNearly++
              leftCube = clickedCube
              checkNearlyCubes()
              console.log(clickedCubes)
            } else if (topCube && clickedCube == topCube && !clickedCubes.includes(topCube)) {
              clickedCubes.push(topCube)
              numNearly++
              topCube = clickedCube
              checkNearlyCubes()
              console.log(clickedCubes)
            } else if (rightCube && clickedCube == rightCube && !clickedCubes.includes(rightCube)) {
              clickedCubes.push(rightCube)
              numNearly++
              rightCube = clickedCube
              checkNearlyCubes()
              console.log(clickedCubes)
            } else if (
              bottomCube &&
              clickedCube == bottomCube &&
              !clickedCubes.includes(bottomCube)
            ) {
              clickedCubes.push(bottomCube)
              numNearly++
              bottomCube = clickedCube
              checkNearlyCubes()
              console.log(clickedCubes)
            }
          } else {
            console.log('That is all!')
            console.log(clickedCubes)
          }
        }
        checkNearlyCubes()
*/